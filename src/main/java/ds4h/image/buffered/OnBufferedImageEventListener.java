package ds4h.image.buffered;


import ds4h.image.buffered.event.IBufferedImageEvent;

public interface OnBufferedImageEventListener {
    void onBufferedImageEventListener(IBufferedImageEvent event);
}
